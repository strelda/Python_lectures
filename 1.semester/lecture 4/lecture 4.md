This is precursor to classe, which they learn in the lext lecture.

## notebooks
- Demonstrate what is  wishlist on the first task. Code it from scratch, at least first few points of the How to design functions process.
- Realize that now we represent the temperature as number, but it would be better to represent it as some datatype Temperature to be safe. We will learn this the following lecture (teaser for classes).
- Draw the temperature boxes on board (as circle with two parts: number and unit) and show how we can use them to solve the problem.

## Homework: 
P1M: Vkládání prvků

They will need this:
```python
n = int(input())
x = []
for i in range(n):
    x.append(int(input()))
```

In the 2. notebook, when converting to html on a white background, it is better to uncomment the png image link and comment the vector graphics.